// あそびかた（村の フェリス・村の メニューから開く）。項目の一覧 → 1項目ずつ見るだけの窓。
//
// 1つの長い窓にすると、スマホでは読みたい所まで巻き取るのが手間なので、項目ごとに分けている。
// 1ページが だいたい画面に収まるくらいの量にする。

import { MAIN_DECK } from "../core/data/items";
import type { Ctx } from "./ctx";
import { infoWindow, listWindow } from "./list";

/** 見出し。 */
const h = (text: string): string => `<h3>${text}</h3>`;

/** キーと説明の表（左の列はキー・ボタンの名前）。 */
const keys = (rows: [string, string][]): string =>
	`<table class="howto-keys">${rows
		.map(([k, v]) => `<tr><th>${k}</th><td>${v}</td></tr>`)
		.join("")}</table>`;

/** 箇条書き（1行ずつ）。 */
const lines = (ls: string[]): string =>
	`<ul class="howto-list">${ls.map((l) => `<li>${l}</li>`).join("")}</ul>`;

const PAGES: { value: string; label: string; sub: string; html: string }[] = [
	{
		value: "village",
		label: "村",
		sub: "保守村",
		html:
			h("歩く・話す") +
			keys([
				[
					"十字キー・タップ",
					"歩く（人を　タップすると　そこまで　行って　話す）",
				],
				["A", "目の前の　人・物に　話す・しらべる"],
				[
					"メニュー（B）・☰",
					"村の　メニュー（冒険の記録・図鑑・あそびかた　など）",
				],
			]) +
			h("だれに　たのむ") +
			keys([
				["レイ・掲示板", "冒険の記録（リプレイ）・売り上げ"],
				["フェリス", "図鑑・あそびかた"],
				["テト", "倉庫（町が　育つと　建つ）"],
			]) +
			h("もぐる") +
			lines([
				"崖の　穴が　ダンジョンの　入口。ふむと　もぐるか　きかれる。",
				"まだ　開いていない　入口は、立て札に　開き方が　ある。",
				"中断した　冒険は、はじめの　画面か　入口の　「つづきから」で　もどれる。",
			]),
	},
	{
		value: "touch",
		label: "そうさ",
		sub: "スマホ",
		html:
			h("ボタン") +
			keys([
				["十字キー", "8方向に　歩く（敵の方へは　向くだけ）"],
				["A", "こうげき・決定"],
				["となりの敵を　タップ", "そちらを　向く。もう一度で　こうげき"],
				["道具（B）", "もちもの を開く（窓の中では　とじる）"],
				["☰", "メニュー（足元・山札・つよさ・図鑑 など）"],
				[
					"矢",
					"装備した　矢を　向いている方へ　1本　撃つ（矢を　装備すると　A の横に　出る）",
				],
				["画面を　タップ", "そこまで　歩く"],
				["通路の先を　タップ", "通路に　そって　走る"],
				["画面を　おさえる", "その方へ　歩きつづける"],
				["キリコを　タップ", "足元を　しらべる"],
				["十字キーの　まん中を　長押し", "足踏み（おさえている間　つづける）"],
			]) +
			h("小さい　ボタン") +
			keys([
				["向き", "おしながら　十字キーで　向きだけ　かえる"],
				["足元", "足元の　道具や　階段を　しらべる"],
				["地図", "この階の　地図を　見る"],
			]) +
			`<p class="hint">向きは　かるく押すと　つぎの1回だけ。<br>足踏みは　敵が　見えたり　傷ついたら　いったん止まる（指を　はなして　もう一度）。<br>十字キーは　せっていで　かくせる（画面を　おさえて歩ける）。</p>`,
	},
	{
		value: "keys",
		label: "キーボード",
		sub: "PC",
		html:
			h("歩く") +
			keys([
				["矢印キー", "2つ　同時押しで　ななめ"],
				["テンキー", "8方向（5で　足踏み）"],
				["hjkl yubn", "8方向（vi キー）"],
			]) +
			h("ボタンの　かわり") +
			keys([
				["Z・Enter", "A（こうげき・決定）"],
				["X・I", "もちもの（窓の中では　キャンセル）"],
				["Esc・Tab", "メニュー（窓の中では　とじる）"],
				["Shift", "押しながら　歩くと　ダッシュ"],
				["R", "押している間　斜め固定"],
				["F", "押しながら　向き変え"],
				[".", "足踏み"],
				["G", "足元"],
				["M", "地図"],
				["T", "投げる"],
				["Q", "装備した　矢を　撃つ"],
				["O", "持ち物を　整理（もちものの　窓でも）"],
			]),
	},
	{
		value: "rules",
		label: "きまり",
		sub: "ターン・満腹度・未識別",
		html:
			h("ターン") +
			lines([
				"1歩　歩くと　1ターン。敵も　同じだけ　動く。",
				"考えている間は　だれも　動かない。あわてなくて　いい。",
			]) +
			h("満腹度") +
			lines([
				"歩くと　満腹度が　へる。0になると　HPが　へっていく。",
				"おにぎりを　食べて　しのごう。",
			]) +
			h("毎回　はじめから") +
			lines([
				"レベルは　毎回　1から。持ち物は　特大おにぎり　1つだけ。",
				"草・スレ・トリップ・杖は、はじめは　名前が　わからない（未識別）。使うか　有識者スレで　正体が　わかる。",
				"のろわれた　装備は　はずせない。",
				"罠は　ふむまで　見えない。",
			]) +
			h("セーブ") +
			lines([
				"とちゅうで　やめても、つぎは　「つづきから」　もぐれる（はじめの　画面か　入口で。自動で　セーブ）。",
				"倒れたら、持ち物も　レベルも　なくなる。やりなおしは　できない。",
				"ダンジョンは　3つ。ちょっと過去ログの底（10階）→　過去ログの底（20階）→　もっと過去ログの底（30階）の　順に　開く。",
				"終わった　冒険は、村の　掲示板・レイか　メニューの　「冒険の記録」から　リプレイで　見返せる（新しい　20回ぶん）。",
			]),
	},
	{
		value: "deck",
		label: "山札",
		sub: "道具は　数えられる",
		html:
			h("山札") +
			`<p>この冒険で　出る道具は、ダンジョンごとに　決まった　枚数の　山札から　配られる（過去ログの底は　全${MAIN_DECK.reduce((a, e) => a + e.count, 0)}枚）。中身は　毎回同じで、並びだけが　ちがう。</p>` +
			`<p>メニューの「山札」で、何を　何枚　見たかが　わかる。数えれば、未識別の道具の　正体が　しぼれる。</p>` +
			h("流れた札") +
			`<p>見ないまま　階を　はなれた札は　「流れた」ことになり、もう　出てこない。</p>` +
			`<p class="hint">モンスターが　持っている　道具も、その階に　配られた札の　1枚。</p>`,
	},
	{
		value: "return",
		label: "帰り道",
		sub: "目的の品を　拾ったら",
		html:
			h("帰り道") +
			lines([
				"いちばん　底で　目的の品（過去ログの底なら　「はじまりの原盤」）を　拾うと、階段が　上り向きに　なる。",
				"帰りの階には　何も　落ちていない。行きで　集めた道具で　のぼりきろう。",
				"帰り道では　満腹度は　へらない。",
				"地上まで　もどれば、冒険は　おしまい。",
			]),
	},
];

export const openHowto = async (ctx: Ctx): Promise<void> => {
	let start = 0;
	for (;;) {
		const v = await listWindow(
			ctx,
			"あそびかた",
			PAGES.map((p) => ({ label: p.label, sub: p.sub, value: p.value })),
			{ start },
		);
		if (v === null) return;
		const i = PAGES.findIndex((p) => p.value === v);
		const page = PAGES[i];
		if (!page) return;
		start = i;
		await infoWindow(ctx, page.label, page.html, { cls: "howto" });
	}
};
